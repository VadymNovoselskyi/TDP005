#ifndef GAME_STATE_H
#define GAME_STATE_H

enum GameState
{
    IN_START_MENU,
    STARTING_GAME,
    CONTINUING_GAME,
    IN_GAME,
    GAME_PAUSED,
    GAME_OVER,
    EXIT,
    // LEADERBOARD,
    // SETTINGS
};

#endif